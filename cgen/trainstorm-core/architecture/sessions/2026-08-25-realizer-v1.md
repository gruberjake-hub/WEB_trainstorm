# Session note — 2026-08-25 Realizer v1

Not canon. Canon is `architecture/DECISIONS.md`.

Jake asked App-maker (this Cursor cloud agent) to build the first course hop: a Realizer that mints
occurrence elements from existing ALSAP atoms and projects a lesson HTML he can open in a browser,
then open a PR. Do not push `main`.

Recorded in `DECISIONS.md`:
- **Realizer v1 exists** (`tools/realize.py`). One occurrence per atom; `ele_` minted at realization;
  `composed_from` links to the atom; no authored meaning on the element; default `move` is `present`.
- Occurrence store is beside the project (`occurrences/`), not inside `atoms.json`. HTML
  (`realized_lesson.html`) reads meaning from the atom.
- 1:many can accrete more elements later without changing atom ids. Cartographer is not a v1 blocker.

Out of scope this session: Couturier, Dragoman, Storyline, `.potx`, `tools/render/` PNG pipelines,
rewriting SOP/form atoms into elements.
