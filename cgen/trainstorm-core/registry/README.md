# registry/  (the expression backing store — git-only, do NOT sync to Project knowledge)
brands/<brand>/  tokens.css · fonts/ · logos/     (local fonts = deterministic on-brand render)
templates/       HTML/CSS layout templates, one per layout_primitive
glossary/<domain>.json   locked terminology (localization registry)
