REHYDRATION SCRIPT

Trainstorm.ai Compiler | Psychological Primitives → Deterministic JSON → Runtime + Pharma Governance

We are building a learner-state-aware course compiler for the Trainstorm.ai HTML/JS runtime. Output must be deterministic JSON (schema-bound), and it must also generate a parallel pharma governance artifact suitable for regulated environments.

Current state

I already have:

A prompt chain that produces near production-ready scripts quickly.

A psychological analysis prompt that extracts:

inhibitors

objections

aligners

identity threats

emotional friction

belief gaps

A motion/language direction (including tokenized motion logic proven in AE), but the target renderer is Trainstorm.ai runtime, not AE/Storyline.

Bottleneck

Manual visual assembly (Storyline/Adobe-style hand build) is the main dragon. We want deterministic compile → runtime render.

Primary goal

Define a compact, powerful set of psychological + structural primitives that enable:

Psychological analysis → primitive extraction

Primitive + intent → deterministic layout/motion/interaction selection

Sequence → learner state transitions (belief shift, resistance diffusion, confidence transfer)

Output → runtime-ready deterministic JSON

Parallel output → governance JSON (pharma/compliance)

Non-goals

Do not produce generic “ID templates”

Do not optimize for flashy motion

Do not collapse into slide-making metaphors

Do not sacrifice intentionality (avoid “canned” output)

Design philosophy

Unit of design = learner state (psychological + intellectual + emotional), not slides.

Automation should compress trivial decisions, not remove human judgment.

Variation should be narrative-responsive, not decorative.

Motion/layout must serve: pacing, attention, identity safety, resistance handling.

HARD CONSTRAINTS
A) Runtime constraint (Trainstorm.ai HTML/JS)

Primitives must compile into deterministic structures that a web runtime can render:

components (text, panels, steps, scenarios)

timing cues (markers/cues)

token-based motion styles (theme tokens)

accessibility hooks (reduced motion, readable states)

B) Deterministic JSON constraint

Output must be:

schema-bound

stable ordering

no “creative” structure drift

explicit fields for: intent, emphasis, tone, pacing, evidence level, risk level

C) Pharma governance layer constraint

For every learner-facing artifact, generate a parallel governance record that can capture:

competency/behavior mapping (if provided)

decision points / branching (if applicable)

risk flags (e.g., compliance sensitivity, ambiguity)

content provenance (source refs / SME notes if available)

completion/attestation hooks (where relevant)

audit-friendly summaries of “what was taught” + “why this structure was used” (high-level)

Governance must be:

deterministic

renderable into LMS/compliance systems

unobtrusive to the learner experience

TASK (Start Here)

Help me define 8–12 core primitives that are BOTH:

psychologically meaningful (belief shift / resistance / identity safety)

structurally renderable (runtime components)

For each primitive, specify:

Name (stable ID)

Psychological function (what state shift it targets)

When to use (triggers from inhibitors/objections/aligners)

Required fields (deterministic JSON fields)

Optional fields (emphasis, intensity, pacing)

Recommended motion/layout intents (renderer-agnostic)

Governance hooks (what to log / flag)

Then define the mapping layer:

inhibitor/objection/aligner → primitive selection rules

intensity scaling (mild objection vs identity threat)

sequencing rules (state transition patterns)

We are designing a learner-state-aware compiler for regulated pharma contexts.

Begin by proposing the primitive set and the mapping rules.