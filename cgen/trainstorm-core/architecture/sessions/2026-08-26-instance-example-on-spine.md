# Session note — 2026-08-26 instance example on the ALSAP spine

Not canon. Canon is `architecture/DECISIONS.md`.

Jake asked App-maker (this Cursor cloud agent) for one worked example on
the short ALSAP lesson after Procedure A and before the checks. The job
aid is how; the instance is that it happened. Use existing instance
atoms (`cgen/astellas/projects/alsap_asp9999`, fictional ASP-9999). Do
not invent facts. Open a PR. Do not push `main`. Claude is a co-builder
— do not freeze them out of files or rewrite their canon.

Recorded in `DECISIONS.md`:

- **Procedure A has no honest match** in the ten instance atoms (plan-
  development acts vs filled AST-34037 values). Two cited atoms
  illustrate the ALSAP generally: selected BR profile
  `conditional_favorable` + the authored rationale.
- **Cross-store join.** Guest `ele_` records in the ALSAP occurrence
  store `composed_from` instance `atom_id`s. Meaning catalog joins the
  sibling store. `ast_alsap/atoms.json` untouched. Instance atoms not
  rewritten into SOP atoms. Clothes: existing `exemplify` /
  `brand.example` / `tp_body`. Not another SOP card. Not all ten
  instance atoms.
- Idempotent with realize → cartographer → couturier. Store 53 / 47.
  No authored `content.text`. No procedure-step MCQ.

Out of scope this session: chameleon.py, audience variants, LLM
distractors, procedure-step MCQ, `/cgen/alsap` hosting, Dragoman,
Storyline, `.potx`, motion, PNG pipelines, procedure B/C on the spine,
inventing a `retrieve` enum, dumping all ten instance atoms.
