[build]
  publish = "."
  command = "cd cgen/lumina && npm ci && npm run build && cp -r out/. ."

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
