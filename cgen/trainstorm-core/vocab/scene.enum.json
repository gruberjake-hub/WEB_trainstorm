{
  "vocabulary_version": "scene.v2",
  "status": "draft_for_build",
  "owner": "realizer",
  "description": "Closed vocabulary of first-class SCENE records. A scene is an ordered grouping of existing ele_ / occurrence refs plus optional in-scene check-shape refs — not a second meaning store, not an LLM path, and not invented outcome language. When present, occurrences/scenes.json is the source of truth; Realizer reads it and stamps the runtime view on spine.scenes (analogous to manifest.lessons) and membership on ext.scene. The grouping heuristic may still propose a default catalog on first mint. The HTML projector READS those records to wrap and page; it does not re-discover scenes by if-atom-id.",
  "governance": {
    "closed_list": true,
    "rule": "A scene record may only use roles defined here. New roles require a version bump and an entry. Headings are the documented title heuristic for that role, not “will be able to…” copy. Membership is ordered element_ids (refs), not copied atom text. In-scene checks are shape refs into manifest.checks, not a parallel pedagogy. Lesson-end definition checks are not a fourth scene.",
    "field": "ext.scene.role and spine.scenes.scenes[].role"
  },
  "roles": [
    {
      "id": "front_matter",
      "label": "Front matter",
      "title_heuristic": "What an ALSAP is",
      "kicker": "Front matter",
      "definition": "Document-root opening, why-this callout of purpose, and teachable front-matter primaries (purpose / scope / general). Those atoms are the SOP’s definitional front-matter.",
      "operands": {
        "element_ids": "ordered ele_ refs already on the spine for this cluster"
      }
    },
    {
      "id": "procedure_a",
      "label": "Procedure A",
      "title_heuristic": "How an ALSAP starts",
      "kicker": "Procedure A",
      "definition": "First Procedures-container branch in object.order (thin heading: A. Plan Development of ALSAP.). Job-aid presents. The projector-only sequence_order check of those presents stays in-scene (a check-shape ref, not a new ele_).",
      "operands": {
        "element_ids": "ordered present ele_ refs of the Procedure A steps",
        "checks": "shape refs into manifest.checks (sequence_order)"
      }
    },
    {
      "id": "form_br",
      "label": "Form",
      "title_heuristic": "Benefit-risk on the form",
      "kicker": "Form",
      "definition": "FORM-AST-34037 BR-field presents plus the instance examples that instantiate those fields. The projector-only closed_choice of the profile fill stays in-scene (a check-shape ref, not a new ele_).",
      "operands": {
        "element_ids": "ordered form-present then instance-exemplify ele_ refs",
        "checks": "shape refs into manifest.checks (closed_choice)"
      }
    },
    {
      "id": "topic",
      "label": "Topic",
      "title_heuristic": "authored heading in the scene record (an expository corpus has no single procedure to name it)",
      "kicker": "Topic",
      "definition": "v2 — a titled expository grouping: ordered statement/section/list presents that teach one coherent chunk of a didactic corpus, plus optional in-scene check refs. The catalog record's authored heading/kicker are designer copy (like every scene heading); membership stays ordered ele_ refs, never copied atom text. First consumer: brunswick/paytrans, the first expository (non-SOP, non-form) course.",
      "operands": {
        "element_ids": "ordered ele_ refs for this topic",
        "checks": "optional shape refs into manifest.checks"
      }
    }
  ],
  "record": {
    "id": "stable scene id (lesson-specific; not a closed enum)",
    "role": "one of roles[].id",
    "heading": "title heuristic for that role — stamped, then read by the projector",
    "kicker": "short role label",
    "element_ids": "ordered ele_ refs; must resolve on the occurrence store",
    "checks": "optional list of {shape, see: checks} pointing at manifest.checks — in-scene projector-only checks",
    "from": "documented heuristic provenance, not outcome language"
  },
  "not_a_scene": {
    "lesson_end_checks": "Definition/purpose invert_definition extras stay at spine.scenes.lesson_end_checks. They are a final player step after the last named scene, not a fourth role."
  },
  "paging": {
    "policy": "v1_one_scene_at_a_time",
    "definition": "Player UX on the first-class scene list: one named scene at a time, Next/Back. Not a second membership heuristic."
  }
}
