# node_modules accidentally staged

## Symptom
git add -A staged thousands of files.

## Root Cause
.gitignore in subfolder and node_modules already tracked.

## Fix
mv cgen/.gitignore ./.gitignore
git rm -r --cached AMLT_Governed_Simulation/.../proxy_server/node_modules
git commit -m "Stop tracking node_modules"

## Guardrail
Root .gitignore:
**/node_modules/
.env
*.env

## Context
AMLT proxy server setup.

## Tags
git, node
